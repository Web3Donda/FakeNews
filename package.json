{
  "name": "fake-news-game",
  "type": "module",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "genlayer deploy"
  },
  "devDependencies": {
    "genlayer-js": "^0.18.3"
  }
}
